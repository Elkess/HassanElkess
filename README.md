# HassanElkess

Simple artist portfolio website with a built-in backend to publish artwork.

## Run locally

```bash
python app.py
```

Then open http://localhost:8000.

## Run tests

```bash
python -m unittest discover -s tests
```
